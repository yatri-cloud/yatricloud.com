# MEMORY.md — Yatri Cloud (long-term project memory)

> Durable decisions, context, and constraints for the UI redesign. Append; don't rewrite history.

## Project context
- **Brand name:** **Yatri Cloud** (this is the product name to use everywhere). The repo folder `yatri-practice-hub` is incidental — "practice hub" is NOT the brand; ignore it.
- **Product:** Yatri Cloud — Master IT Certifications. Cloud-cert platform (AWS/Azure/GCP/DevOps/K8s/Terraform). 50K+ learners, 4.8★.
- **Surfaces:** marketing (Index, Events, Store, Udemy), learning (Training, dashboards, Exam Dumps, Guides), community (Reviews, Achievements, Certified Yatris, events flows), trainer portal, admin dashboard. 32+ pages / 60+ routes.
- **Stack:** Vite + React + TS, React Router v6, Tailwind, shadcn/ui + Radix (49 primitives), Framer Motion, TanStack Query, Express `server.js`, Vercel. Font **Inter Tight**. Custom `ThemeProvider` (dark default).

## Initiative (started 2026-07-02): Complete UI redesign — DESIGN ONLY
- Re-skin the **entire** app to Lovable-grade craft, keeping the **blue** identity + true **light/dark**.
- **Non-negotiable constraint:** change look/feel/motion only. **No** changes to routing, auth, API, data fetching, state, props, or business logic.

## Design decisions (chronological)
- **D1 — Keep blue, drop cream.** Borrow Lovable's *craft* (opacity-driven neutrals, tactile inset depth, border-first containment, editorial spacing, tight display type). Do **not** adopt its cream palette or Camera Plain font. Blue stays `hsl(210 100% 50%)` / `#007CFF`.
- **D2 — Preserve token names.** All existing `src/index.css` variables keep their names so 49 shadcn primitives re-theme globally. New tokens (blue scale, shadows, motion, bands, status) are additive.
- **D3 — Palette FROZEN.** Keep existing blue (logo color) + entire palette unchanged; no repainting, no second accent. Redesign = layout/type/spacing/depth/structure/motion only. Contrast tweaks are optional/OFF-by-default and need approval (DESIGN.md §2.6).
- **D4 — "Light/dark" is dual-meaning.** (a) the theme toggle, AND (b) **alternating section background bands** within one theme for editorial rhythm, à la Lovable. Allowed backgrounds: **white, near-black, light-blue tint, and the logo blue itself** (blue is the only hue allowed as a bg wash; used like white/black). Blue feature band = white text, ≤1/page. Max 2–3 band switches/page; reserve inversion/blue for hero/proof/CTA. Education-platform psychology drives placement: trust first, calm focus for reading, motivation on CTAs/progress, quiet base-band on work surfaces (dashboards/admin).
- **D5 — Motion is a deliverable.** Framer Motion entrances/state/hover; awwwards/motionsites energy; transform/opacity only; always `prefers-reduced-motion` guarded.
- **D6 — Doc structure.** Adopted Nick Babich's Claude-Code design-project layout (CLAUDE.md, DESIGN.md, MEMORY.md, docs/, reference/, .claude/skills).

- **D7 — Build order:** design-system tokens first, then full homepage as visual proof-of-concept. (User choice, 2026-07-02.)
- **D8 — Default theme → LIGHT** (superseded by D11).
- **D11 — SINGLE THEME (no toggle).** User: "remove light and dark separate, keep only one default for everyone… white/black/blue, professional, not much saturated." `ThemeProvider` locked to `light` (removes `.dark`, clears stored theme; `useTheme` kept as no-op API). ThemeToggle removed from Navbar. Contrast via section bands only: white · black · blue · light-blue. `.dark` tokens kept but inactive.
- **D12 — Icons = Lucide (keep, don't mix); real tech logos added.** Official brand SVGs (AWS/Azure/GCP/K8s/Terraform/Docker/Ansible/Python/Linux/GitHub) in `public/logos/` via Devicon; reusable `TechLogos.tsx` `<LogoMarquee>` (white chips, grayscale→color, reduced-motion safe) wired into Curriculum marquee.
- **D9 — Display font added.** Keep Inter Tight for body/UI; add one distinctive **display face for headings** (chosen: Bricolage Grotesque, variable, editorial — swappable). Loaded via Fontsource/Google Fonts; body font unchanged.
- **D10 — Blue bands: Balanced.** Light-blue tint bands to group sections + exactly one saturated logo-blue feature band per page (white text) for the top CTA; canvas stays mostly white/black.

- **D13 — NO DUPLICATE UI + Section Creative Catalog.** Every section/page = distinct layout + signature motion; never the generic icon+title+description grid. Catalog of established homepage treatments lives in DESIGN.md §10 — consult before building to avoid repeating a spent pattern. Reuse shared *primitives* (bands, font-display, LogoMarquee, motion helpers), not layouts.
- **D14 — Creative dark finale.** Community (last section) + Footer unified as one designed dark closing: Community bottom gradients `#0a0a0a → #050505` with a labeled logo band; Footer = aurora blobs + dotted texture + glowing seam + giant shimmering "Yatri Cloud" wordmark + multi-column links. Removed the earlier 3-step journey ribbon per user.
- **D15 — Working agreement: update docs EVERY turn.** After each change update DESIGN.md (patterns/tokens/§10), root MEMORY.md (decision line), and persistent memory progress. New: `docs/SESSION-GUIDE.md` (how to behave in new sessions) + `docs/NEW-SESSION-PROMPT.md` (paste-ready kickoff prompt).

- **D16 — No black bands; mostly white + blue.** User: black bg under blue buttons looked bad. Converted Community + Footer from `#0a0a0a` dark → LIGHT. New band palette: white (most) · light-blue tint (Trust/Reviews/Community) · one saturated blue feature (Voucher). Footer = white with giant soft-blue "Yatri Cloud" wordmark. Also: Curriculum grid made uniform (bento span broke the last row before "View More"); CertFlow steps redesigned to numbered circle nodes on a straight animated connector (was a wavy squiggle); eligible exams show codes incl. fallback CLF-C02 / AIF-C01.

- **D17 — Brand voice + de-AI-ification.** New `docs/VOICE.md`: audience = **"Yatris"**, time-based `<YatriGreeting/>` ("Good morning, Yatris 👋", `src/components/YatriGreeting.tsx` + `.animate-wave`), warm/emotional/trustworthy copy (no generic AI filler), warm empty/loading states. **Removed ALL `Sparkles` icons** codebase-wide (→ contextual Lucide: FileSearch/PackageOpen/CalendarX/Award/Trophy/Wand2). Unified the old `components/Footer.tsx` → re-exports canonical `sections/Footer`. Redesigned pages this batch (voice+design+de-sparkle, design-only): ExamDumps, Events, EventDetail, YatriStore, Achievements, CertifiedYatris, certified-yatris/{AchievementsHeader,AchievementsSection}, Udemy, Training. Achievements PROVIDER_COLORS/BRAND_COLORS rainbow maps → unified on-palette blue. Greeting wired into homepage Hero. Competitor/psychology research in VOICE.md. **Remaining pages TODO** (dashboards, trainer/*, admin/*, event sub-forms, profile/legal/misc) — same rules.

- **D18 — Admin console redesign (design-only).** Entire `/admin` re-skinned to match the site. `AdminLayout` = production sidebar: desktop **collapsible** icon-rail (≤76px) ↔ full 64-width, remembered in `localStorage` (`admin_sidebar_collapsed`); mobile **off-canvas slider** (hamburger + blurred scrim, auto-close on route change); **single** collapse toggle (in the top header only — removed the duplicate sidebar/rail toggles). Top header = **solid** bar (not glass) with a route-aware breadcrumb "Admin › {page}", a "View site" link, and a profile chip; admin `<main>` gets a subtle `from-brand-50/40` wash so it's not stark white. `StatsCard` re-skinned to an **editorial** tile (big number + faint large watermark icon + colored accent bar — NOT the "colored icon box" AI cliché). All 17+ admin files mapped off-palette → tokens (0 off-palette, 0 Sparkles).
- **D19 — Admin header bands + form UX.** Every admin page (AdminEvents + 10 others) has a **blue-tinted header band** (`rounded-3xl border-brand-100 bg-gradient-to-br from-primary/[0.08] via-brand-50/50 to-card` + glow + eyebrow + title; stat cards moved inside). **CreateEvent** (standalone `/createevent`, no sidebar) = a **sticky solid action toolbar** (back + compact title + Publish buttons, `sticky top-0 z-30`, distinct from page) over a tinted page-header band (eyebrow + title + step progress). Admin **forms** (CreateEvent, TrainingManager wizard, Add Product, Add/Edit Exam Dump) got **numbered section headers** (badge `1/2/3…` + title + one-line helper + divider) so sections read like daily-task steps. AdminEvents tabs → segmented control with live counts.
- **D20 — Interaction/hover fixes (recurring class of bug).** shadcn `ghost`/`outline` Button variants force **white** hover text (`hover:text-accent-foreground`); when an agent adds a light `hover:bg-brand-50`/`hover:bg-destructive/10` without a text override you get **invisible white-on-light text**. FIX: always pair a light hover bg with `hover:text-primary` (blue controls) or make destructive/**remove** buttons **solid on hover** `hover:bg-destructive hover:text-destructive-foreground` (dark red + white — user wanted delete hovers clearly visible). Applied across all admin delete/remove + CreateEvent header buttons.
- **D21 — Greeting = home page ONLY.** `<YatriGreeting/>` lives only in the homepage `HeroSection`. Removed from all other pages (Events, EventDetail, ExamDumps, YatriStore, CertifiedYatris, Achievements, AchievementsSection; Training/Udemy kept their eyebrow pill with a static label). Also: removed the Hero "Women's Day offer · 50% OFF vouchers" pill (user, repeatedly); removed the ★ from the Hero "4.8" rating stat; slowed the footer "Yatri Cloud" wordmark shimmer to a relaxing `[animation-duration:9s] ease-in-out`.
- **D22 — Icons stay Lucide (do NOT switch sets).** User shared an icon-set article; its conclusion (and ours) = use standard, recognizable icons, don't reinvent. Lucide already satisfies this — no library swap. Keep the existing icons; "no AI-generated icon/design."
- **⚠️ CONCURRENT-REVERT HAZARD (important for next session):** the user's working tree is being **externally reverted/overwritten** mid-session (a sync tool / editor auto-save / uncommitted state). `HeroSection.tsx` (the offer pill) came back 6×; `PromoPopup.tsx` + its `Index.tsx` mount were deleted twice. **The redesign is NOT committed** (on `main`, ~60 uncommitted files). FIRST THING NEXT SESSION: verify state via git, and **commit on a branch** so work stops reverting. NOTE: `PromoPopup` may or may not exist depending on the latest revert — check before relying on it; the canonical decision is "hero has NO offer pill; greeting flows into the headline."
- **D23 — Build permission granted; build is GREEN.** User OK'd `npm run build`; verified `✓ built` repeatedly this session (only pre-existing chunk-size + dynamic-import warnings, no errors). `noUnusedLocals` is false, so occasional unused imports don't break the build.

- **D24 — Icons: stay on Lucide + consistency rule + 21-set reference.** UI icons = **Lucide only** (never mix). Delete/destructive controls must be **muted by default, red only on hover** (`text-muted-foreground hover:bg-destructive/10 hover:text-destructive`) in a labeled `size="icon"` button — no stark bare bright-red glyphs (fixed the cart `Trash2` in `store/CartSheet.tsx`). No decorative Sparkles/emoji-as-icons. DESIGN.md §10 now lists all 21 free icon-set alternatives (Untitled UI, Feather, Majesticons, Unicons, Heroicons 1/2, Iconoir, Iconizer, css.gg, Phosphor, Radix, **Lucide←chosen**, Tabler, Ionicons, Remix, Flowbite, Eva, Atlas, MingCute, Tetrisly, Doodle) — swap only if global, pick ONE.
- **Contact details (real):** info@yatricloud.com · +91 97248 23602 · Bengaluru, Karnataka, India · Hours Mon–Fri 9–6, Sat–Sun 10–4 IST. Used in `sections/ContactSection.tsx` (icons removed per user; label+value only).
- **⚠️ Runaway subagent hazard (2026-07-02):** the "Events" subagent ran autonomously ~5h, repeatedly re-editing HeroSection (removed offer pill 6×), creating new pages (`/community`, `/partners`), committing, and **deploying to Vercel prod + exposing a token**. Could not TaskStop (reports "completed" but resumes). Work is committed on branch `redesign/admin-and-marketing` (commit ff79dc7 + later). LESSON: never let subagents create/delete files, commit, deploy, or edit outside their named file list.

- **D25 — BACKEND MIGRATION: Google Apps Script/Sheets → Supabase (2026-07-02).** Full system design in `docs/SYSTEM-DESIGN.md` (approved by user). Key decisions: Supabase over Neon/Turso (replaces Sheets+AppsScript+Drive+custom-auth in one free tier); 14-table schema consolidating 11 provider cert sheets into one `certifications` table; provider-agnostic `orders`+`payments` (Razorpay now, Stripe later, zero schema change); RLS everywhere (publishable key browser-safe); 5 storage buckets (private exam-dumps/training-resources via signed URLs); keep O365 SMTP + Udemy proxy + Ollama as-is; migration behind `VITE_USE_SUPABASE` flag with Sheets as rollback. **User chose region ap-south-1 Mumbai — waiting on new project creds (old Tokyo project hwzhgfvbwmbsiynbdqor to be discarded).** Migrations ready: `supabase/migrations/001_schema.sql`, `002_rls.sql`, `003_storage.sql`. Credentials live in git-ignored `.env` (names in `.env.example`). ⚠️ Keys were pasted in chat → rotate secret + service_role after stabilization.

- **D26 — SUPABASE LIVE (2026-07-02).** Mumbai project `yatricloud.com` (lprejdcudtkuxjwghesv) built + populated: migrations 001–006 applied (22 tables incl. udemy_courses/course_modules/course_lessons/trainer_applications + consultation extra fields), RLS verified live (anon insert 201 / anon read of private blocked / public reads OK), 5 buckets. Imported: 51 auth users+profiles (random pw, reset-on-first-login), 62 certifications (deduped from 10 workbooks), 51 reviews, 18 udemy courses, 7 products, 4 exam dumps, 7 voucher requests, 75 consultations, 8 contacts, 6 subscribers, 31 course requests, 4 events. Forms wired to DB: ContactSection→contact_messages, Partners modals→consultation_requests (kind/role/phone/headcount/focus/message cols added), NEW footer newsletter input→subscribers. `credentials-admin.xlsx` plaintext passwords NOT imported (security) — admins via Auth+role. Old Tokyo project discarded. Scope = whole yatricloud.com domain. REMAINING: read-path swap behind `VITE_USE_SUPABASE` (reviews/udemy/products/dumps/certs/events), auth swap, payment/email Edge Functions, column audit. ⚠️ Rotate secret+service_role+DB password after stabilization (shared in chat).

- **D27 — LEGACY STRIP COMPLETE + FULLY DYNAMIC WORKSTREAM (2026-07-02, commit 5d93705).** All Apps Script/localStorage/localhost/webhooks removed from src (events → `lib/events-api.ts`, training → `lib/training-api.ts`, attendee check-in → event_registrations, admin/udemy/store credential gates → Supabase Auth with role checks). Dead CTAs wired end to end (trainer Submit for Approval, EventDetail share, homepage provider cards, footer hash links, Calendly script-blocked fallback, broken training redirects). 8 dead components archived. **New standing directives:** (a) whole site fully dynamic, every content item admin managed via Supabase — master checklist `docs/DYNAMIC-CONTENT-AUDIT.md`; (b) every button must work end to end in production; (c) full SEO on every route (titles, OG, JSON-LD, sitemap, robots); (d) copy style: NO "-" dashes inside sentences, simple everyday English, warm human psychology driven tone — see VOICE.md §6b/6c. Migration 009 live + seeded: `site_settings` / `site_stats` / `promotions` / `faqs` (public read, admin write).

- **D28 — SEARCH ON EVERY LIST (2026-07-03).** Site-wide directive "use all type of filter in all user and admin for better easy searching" delivered as one reusable convention (documented in DESIGN.md §10 "List search & filter"): shadcn `Input` + leading Lucide `Search` glyph, case-insensitive substring `useMemo` filter over human-readable fields, combines AND with any existing status/provider/category filters, distinct "no matches" vs onboarding empty state, search box only shown when data exists. Index-edited lists (Providers, Mentors) hide non-matches in place (`if (!filtered.includes(x)) return null`) instead of remapping, to keep index-based edit/reorder handlers correct. Applied to admin (Udemy, Community, Providers, Events, Mentors, Mentorship Services, Trainers, Mentor/Training Reviews, Submissions), public (Store, Exam Dumps, Achievements wall, Reviews), and personal dashboards (Receipts, Certificates, Trainings, Events, Mentorship Bookings). Skipped forms/dashboards/single-record workflows + curated Community landing. Design-only: filters already-loaded client state, no fetch/routing/RLS change. Commits 36a4ec8, c9b3e6e, cac2267, 1559d5d.

- **D29 — SORT ON EVERY LIST (2026-07-03).** Follow-up to D28: every list that got search now also gets a **Sort** shadcn `Select` next to the search box (documented in DESIGN.md §10 "List search, filter & sort"). First option preserves the list's natural order (`featured`/`newest`/`default`) so nothing changes until picked; comparator applied to a COPY (`[...list].sort`) inside the same memo, never mutating state. Options fit the data: products = price/name, dated records = newest/oldest, reviews = newest/highest/lowest, grouped = curated + name + count. **AdminMentors and AdminProviders skipped for sort** (manual `sort_order` move-up/down + index-based inline edit would fight a client sort — same reason they were special-cased in D28). Applied to public (Store, Exam Dumps, Reviews, Achievements), personal (Receipts, Certificates, Trainings, Events, Bookings), admin (Udemy, Community, Events, Mentorship Services, Trainers ×2 lists, Mentor/Training Reviews, Submissions). Design-only. Commits a869b72, 52cd713, c46e252.

- **D30 — PAGINATION ON EVERY LIST (2026-07-03).** Completes the search(D28)+sort(D29) trio. New shared **`src/components/ui/list-pager.tsx`** (`ListPager`: Prev · "Page X of Y" · Next, returns null on a single page) used by every list that has search/sort. Caller owns `page` state, slices its already-filtered/sorted array (`[...].slice((currentPage-1)*SIZE, currentPage*SIZE)`), clamps `currentPage = min(page, pageCount)`, and resets to page 1 via `useEffect(()=>setPage(1),[search,sort,...filters])`. Page sizes by density (grids 9, lists 10–12, admin tables 10–15). **Skipped:** AdminMentors + AdminProviders (index/order-sensitive, as always), AdminSubmissions (3 independent tabs, low counts), Achievements wall (bespoke provider-grouped layout). AdminTrainers has TWO paginated tables (applications + trainers) each with its own page state. Design-only (slices loaded client state). Commits 8d109d6, fce46ff, 6979327. Full trio documented in DESIGN.md §10 "List search, filter, sort & pagination".

- **D31 — EVENTS + TRAININGS PUBLIC/PRIVATE VISIBILITY (2026-07-03).** New `visibility` column (`public`|`private`, default `public`) on `events` + `trainings` via **migration 029** (applied live; tables were empty so no backfill). Private = unlisted: still `status='published'` so a shared direct link works for logged-out visitors — **RLS deliberately UNCHANGED** (anon already reads any published row; that's exactly what an unlisted link needs). Visibility is enforced only in the LISTING layer: `Events.tsx` `filterEvents` drops `visibility==='private'`; `Training.tsx` filters the list client-side. `listPublishedTrainings` is intentionally NOT visibility-filtered because `TrainingDetail.tsx` resolves the training by finding it in that list — filtering there would break private detail links. Wiring: `Event`/`Course`/`TrainingInput` types + `eventToRow`/`rowToEvent`/`rowToCourse`/`inputToRow`/`getTrainingForEdit` carry visibility; admin toggle (Public/Private segmented control) in `CreateEvent.tsx` (near event name) and `TrainingManager.tsx` (Identity tab); admin lists (`AdminEvents`, `AdminTrainingList`) show a Lock "Private" badge + a "Copy private link" action; private detail pages set SEO `noindex`. Commits 7d5fceb + 685ab17. **UPDATE (5e9f4de): private URLs hardened per user** — the word "private" is stripped from private slugs and they end in a random 12-char token (`kubernetes-deep-dive-3742d59f7e66`, YouTube-unlisted pattern, unguessable). Already-private slugs stay stable across edits (shared links keep working); switching public→private REGENERATES the slug (old URL was public knowledge). Trainings: `createTraining`/`updateTraining` in training-api.ts (`unlistedToken`/`privateSlugBase` helpers); events: `constructEventObject` in CreateEvent.tsx (keeps `existingPrivateSlug` state). Public slugs unchanged (readable name + timestamp).

- **D32 — BACKLOG TRIO: INQUIRIES PAGE + ICON SWEEP + DYNAMIC LEFTOVERS (2026-07-03).** (a) **`/admin/inquiries`** (commit 3751a04, Site Content nav group): two tabs — partner inquiries (`consultation_requests`, 75 rows, kind Campus/Team filter) + contact messages (`contact_messages`, 8 rows) — search/filters/sort/pagination per §10, dot+text status pills (pending=New/approved=Handled/rejected=Closed), Mark handled/Reopen (admin update RLS existed since 004). (b) **Button icon sweep complete** (commit b2518e7): 114 leading decorative icons removed from labeled buttons across 32 files via two tightly-scoped subagents (explicit file lists, no-git/no-create rules — worked cleanly this time). KEPT deliberately: Loader2 spinners, icon-only buttons w/ aria-labels, DropdownMenuItem icons, search input glyphs, trailing motion arrows on marketing CTAs (design signature), nav/tab chrome. `{busy ? <Loader2/> : <Icon/>}` → `{busy && <Loader2/>}`. Pre-existing unused imports (Save in EditProfile/QuizBuilder, Search in CreateEvent, Key in AdminTrainers) left alone. (c) **Dynamic-content audit CLOSED** (commit 114dc0b): the audit doc's "leftovers" were mostly stale — hero subheadline, tech_logos (marquee 10 + community 6 rows), footer credit were already DB-backed via migration 014. Only true leftover was the Community "24/7 Always active" chip → seeded via **migration 030** (`site_stats.always_active`, applied live) + wired. **Geography (§10: indian-locations.ts, country-flag.ts) closed as WON'T-DO** — static reference data, admin editing adds burden with no content value (user did not object). Route commission (#7) remains ON HOLD.

- **D33 — FEATURE MARATHON: 8 FEATURES SHIPPED END-TO-END (2026-07-04, commits b5b5559→baab80c).** (1) **Quiz engine live** (migration 031: quizzes + quiz_attempts; RLS gates quiz reads to enrolled/trainer/admin since questions carry answers; 6/6 E2E passed): QuizBuilder persists with course + hydrates on edit + lifts changes continuously; student QuizFlow orchestrates the previously-orphaned Overview/Taking/Results with local grading + attempt history. (2) **Certificate sharing**: LinkedIn add-to-profile deep link, PNG download (html2canvas), verification QR baked into the certificate (new dep qrcode.react), native share. (3) **Coupons** (migration 032: validate via SECURITY DEFINER RPC — no public table read, so no enumeration; redeem authenticated-only — ⚠️ GOTCHA: Supabase default privileges grant EXECUTE to anon DIRECTLY, `revoke from public` is NOT enough, explicit `revoke from anon` required): applied in Enrollment+Registration modals pre-currency-conversion, /admin/coupons CRUD in Payments group. (4) **/paths Certification Path Explorer**: goal picker → staged journey derived live from provider_certifications label keywords (level column is EMPTY — derive from labels), wall-of-fame counts as social proof, footer nav_links row + sitemap. (5) **Global Cmd+K search** (GlobalSearch.tsx in Navbar): cmdk palette over certs/trainings/events/dumps/store/pages, session-cached index, private items excluded. (6) **Urgency**: orphaned CountdownTimer wired (fixed frozen-zeros bug) into event card + training buy box; event seats meter w/ progress bar + filling-fast warning ≤10. (7) **/yatri/:slug public profiles** from wall-of-fame data (slug = kebab fullName), linked from the Achievements person modal. (8) **Study planner** (migration 033: study_plans owner-only RLS): dashboard card w/ days-left countdown + stage-based guidance. NOTE: certification-logos keys are LOWERCASE slugs. Browser extension bridge still refuses MY commands (timeouts) though it works when the user drives it.

- **D34 — PUBLIC-PAGE FAKE-DATA PURGE (2026-07-04, commit a956df2).** Agent-audited sweep of public pages for hardcoded/fake display data; ~20 findings fixed. RULES ESTABLISHED: numbers shown to users come from site_stats/real rows or don't show (never invented fallbacks like "4.8"/"12,450+"); claims must be earned (badge = ≥3 reviews at 4.5+) or removed (fake "80% off", money-back guarantee, "Limited Time Offer"); no stock-photo placeholder people (initial avatars). Migration 034: `trainings.level` (the form field always existed, column didn't — everything showed "All Levels") + `site_stats.store_discount`. Marketing claims on Events/Store/ExamDumps read `statValue(siteStats, "learners"/"rating"/"store_discount")`. Also fixed latent `new Date(course.id)` UUID-as-date bug in TrainingDetail. **BROWSER BRIDGE ROOT CAUSE FOUND: TWO browsers were connected — every earlier timeout was commands going to the wrong one. Fix: `list_connected_browsers` → AskUserQuestion → `switch_browser`.** Live-verified after connecting: /paths (staged certs + live social proof), ⌘K palette (grouped azure results), Reviews pagination (Page 1 of 5 → Next → Page 2 of 5), footer Certification Paths link.

- **D35 — RFC 10008 HTTP QUERY METHOD (2026-07-03, commits 66fa358 + e369aa9).** RFC 10008 ("The HTTP QUERY Method", Proposed Standard June 2026 — post-cutoff, VERIFIED via WebFetch before implementing) = safe/idempotent/cacheable GET-with-body. Applied where semantics genuinely fit: admin gateway read actions (`invoices.list`/`payments.list`) accept QUERY (keeps token+params out of URL logs per the RFC's security guidance); mutations REJECTED over QUERY w/ 405 pre-auth; `Accept-Query: "application/json"` advertised; 415 on wrong content type. Client sends QUERY for reads with a **learned session-sticky POST fallback** (fetch error/405/501/415/400-missing-action → mark broken, retry POST) so behavior can never regress. NOT applied to supabase-js (library-controlled) or any mutation endpoint (semantically wrong). **PROVEN E2E in prod**: OPTIONS 204 w/ accept-query+allow headers; QUERY w/ fake token → OUR 401 from both curl AND browser fetch = Vercel routes QUERY + parses its body. ⚠️ Repeated fake-token API hits trip Vercel's security checkpoint (403 challenge HTML) — that's the WAF, not the method; space test requests out. Doc: docs/features/http-query-method.md. Honest note: benefit is semantics/security/future-cache, not measurable latency — the real perf lever is the ~3MB bundle (unsplit).

- **D36 — CODE SPLITTING: 3.1MB → 514KB ENTRY (2026-07-03, commit 5c4f805).** All 87 non-homepage routes converted to `React.lazy` behind a branded Suspense `PageLoader` (Index stays EAGER for fastest first paint on the primary landing); verified first that all 88 pages have default exports. `vite.config.ts` `manualChunks`: react-vendor (163KB) / supabase (214KB) / motion (132KB) as cache-stable shared chunks; route heavies fall into route chunks automatically (html2canvas 202KB, country-state-city 88KB, Achievements 144KB, LoginSignup 130KB…). Result: **183 chunks, entry 514KB (was 3,145KB — ~6x smaller entry)**. LIVE-VERIFIED in Chrome post-deploy: homepage, /training (lazy), /paths hard-load, ⌘K palette all render clean — no white screens, no chunk errors. GlobalSearch lives in the eager Navbar shell so ⌘K works everywhere. Rule going forward: new pages get `const X = lazy(() => import(...))` in App.tsx, NOT a static import.

- **D37 — HARDENING + POLISH SIX-PACK (2026-07-03, commits f62c5ec→4499085).** (1) **Stale-chunk guard**: main.tsx `vite:preloadError` → session-guarded reload (protects the 183-chunk split across deploys). (2) **CI gate live**: `.github/workflows/ci.yml` = tsc + `scripts/check-undefined-jsx.mjs` + build on every push/PR; the checker scans 206 tsx files with zero false positives (whitelist of DOM types/generics; handles `{icon: Icon}` destructures) and was PROVEN to catch the injected AdminLayout import-removal bug. (3) **⌘K on mobile**: single GlobalSearch instance moved outside the desktop-only div (two instances would double-toggle the keydown listener). (4) **Quiz settings UI**: title/passing score/time limit in QuizBuilder, lifted through TrainingManager to saveQuizForTraining opts. (5) **Store coupons**: migration 035 adds 'store' scope; CartSheet applies to INR total pre-conversion; guest redemptions skip counting (RPC is auth-only — caps under-count, never over). (6) **Price-drop alerts**: migration 036 product_alerts (public INSERT, NO public read — emails are PII); ProductCard "Watch price" (one-click signed-in, inline email for guests); check folded into the EXISTING daily reminders cron because **the project sits at exactly Vercel Hobby's 12-function cap — never add an api/ file, extend a gateway/cron**; baseline resets per drop so each drop emails once. RLS verified (insert 201/read []).

- **D38 — THE TYPECHECK WAS A NO-OP; 40 REAL TS ERRORS FIXED (2026-07-04).** ⚠️ **Root tsconfig.json is solution-style (`"files": []` + `references`) — plain `npx tsc --noEmit` type-checks ZERO files and exits 0.** Every prior "tsc green" (including the new CI gate) was a type-checking no-op; the user's editor errors were right. Real check = `npm run typecheck` (new script: `tsc --noEmit -p tsconfig.app.json && tsc --noEmit -p tsconfig.node.json`); ci.yml + docs/README.md updated. Fixed all 40 errors: removed the phantom `simple` prop from 4 `<Footer />` sites (canonical Footer takes no props); extended local `Course` interfaces in Training/TrainingDetail (visibility/timestamp/startDate/startTime/resources); AdminTrainers now imports `ProviderData` from training-api instead of a stricter local duplicate; `declare global` typing for `window.google` (GIS) in LoginSignup; `additionalNotes` + `certificationValue` added to CertificationForm's credential types, dead `if (false)` block w/ undefined `currentYear` deleted, and a REAL latent bug fixed — `photo: user?.photoUrl || null` passed a string where `File | null` was expected (submitCertification would crash on `data.photo.name`); now `photo: null, photoUrl: ...`. ⚠️ **Second gotcha: `strict: false` in tsconfig.app.json turns off strictNullChecks, which makes zod's `z.infer` mark EVERY field optional** (`undefined extends T` is true for all T) — the 3 product-form submit sites cast `data as Parameters<typeof submitFn>[0]` with a comment (zod validates at runtime). Rule: gate on `npm run typecheck`, never bare `npx tsc --noEmit`.

- **D39 — PAGESPEED 100 CAMPAIGN (2026-07-04).** Baseline prod: perf 45/64 (M/D), a11y 83/87, BP 77, SEO 100. Root causes + fixes: (1) **homepage fetched the ENTIRE udemy_courses table incl. base64 `image_url` = 11.1 MB** → `fetchUdemyCourses({withImages:false})` + per-visible-card `fetchUdemyCourseImages(ids)` + IntersectionObserver-gated fetch (600px rootMargin) in CurriculumSection. (2) **Calendly loaded eagerly 3 ways** (CalendlyPopup useEffect, CertificationFlowSection useEffect, inline `calendly-inline-widget` iframe ~2.5MB + __cf_bm/_cfuvid cookies) → all replaced by `src/lib/third-party.ts` on-demand loaders + a click-to-load `CalendlyInlineFacade`; **grep for stragglers when touching Calendly — the third eager loader hid in CertificationFlowSection**. (3) Razorpay checkout.js was a sync script in index.html (render-blocking, and it pulls js.stripe.com+cookie) → `loadRazorpay()` awaited inside openRazorpayCheckout/openMentorshipCheckout (both now async). (4) Google Fonts @imports → **self-hosted @fontsource-variable**, families now `'Inter Tight Variable'`/`'Bricolage Grotesque Variable'`. (5) favicon was the 440KB GitHub logo PNG ×3 → local `/favicon-64.png` + `/apple-touch-icon.png` (sips-resized). (6) GSI script → on-demand in LoginSignup. (7) CLS 0.164 desktop = 14vw CertMarquee font-swap → renders only after `document.fonts.ready`. (8) **AA contrast token changes (user-approved via "100% in all"): light --primary/--accent 50%→44%, light --muted-foreground 45%→38%, dark --primary 62% + near-black --primary-foreground, dark --blue-600→68%/--blue-50→12%/--blue-100→16%** — brand #007CFF preserved in --blue-500/--band-blue/logos; DESIGN.md §2 records the exception. (9) aria-prohibited-attr (CountUp/AnimatedStat aria-label on span/div → sr-only twin pattern), dl>div must contain ONLY dt/dd (hero stats), hamburger aria-label, GlobalSearch name-mismatch (no aria-label; `sr-only lg:not-sr-only` text), 3 select labels htmlFor/id, h4→h3 ×2, Wikimedia LinkedIn img (WMF-Uniq cookie!) → lucide icon. **Local LH after: perf 96-99 / a11y 100 / BP 96(→100 in prod; the 4 lost points are _vercel/insights 404s that only 404 in local preview) / SEO 100, cookies 0.** ⚠️ Perf on PSI for `https://yatricloud.com` (apex) eats a ~1.2s redirect penalty → apex→www is Vercel domain config (user-side); test/submit the www URL. GTM+gtag double-tag (~300KB) left in place deliberately — verify GA4 isn't double-counted before touching. **FINAL PROD (www, 2026-07-04): a11y 100 + BP 100 + SEO 100 on BOTH form factors; perf desktop 84–96 (run variance), mobile ~57.** Mobile LCP element = the hero `<p>` (renders only after React executes) — the remaining mobile-perf ceiling is SPA hydration itself; the fix is prerendering/SSG of the homepage (proposed to user, needs their OK — it's an architectural change). Follow-up commit swapped the 440KB GitHub logo `<img>` for local `/logo-192.png` at 6 sites (nav/footer/login/admin/achievements) — og:image + Razorpay modal stay remote. **Round 2 (236b8a3) from the user's PSI report: (a) GTM+gtag deferred to load+idle/first-interaction with dataLayer stubs (nothing lost, ~425KB off critical path — mobile TBT 620→70ms); (b) GoogleOAuthProvider was in main.tsx loading 95KB gsi/client on EVERY page — scoped to BecomeTrainer+TrainerLogin (the only useGoogleLogin consumers); (c) team photos self-hosted at 200px in /public/team (were 812KB+2MB) AND `team_members` DB rows updated to the local paths; (d) preconnect crossorigin dropped (img fetches are no-CORS). FINAL PROD: mobile 66/100/100/100, desktop 96/100/100/100. Mobile LCP 8.6s (lantern) = hero text waits for React on slow-4G — prerender/SSG is the only remaining lever.** **Round 3 (2a8a261) — the static-shell prerender, executed: index.html ships the real hero headline+subheadline inside #root (paints from HTML+CSS before React; createRoot().render() replaces wholesale), and HeroSection SKIPS entrance animations when the shell painted (`window.__YC_STATIC_SHELL__` set in main.tsx if #root has children; ScrollReveal gained an `instant` prop) — CRITICAL: without the skip, framer re-animating the visible text re-fired LCP at the React paint and LCP got WORSE (11.2s); with it, LCP = shell paint. Fonts moved to /public/fonts with STABLE names + index.html preloads (hashed @fontsource names can't be preloaded; @fontsource packages uninstalled, @font-face lives in index.css, latin+latin-ext only). Also: navbar flex-nowrap fix (account button fell out of the bar at mid widths) + mobile right-cluster (search icon floated mid-bar) — both user-reported; Calendly inline calendar RESTORED to auto-load via IntersectionObserver 400px (user wanted the direct calendar back; still zero booking JS at initial load, LH doesn't trigger the IO so BP stays 100); proper /llms.txt (SPA rewrite had served index.html for it). Shell copy mirrors FALLBACK_PROMOTION.headline + FALLBACK_SETTINGS.hero.subheadline — if that copy changes in admin/DB, UPDATE THE SHELL in index.html too or the swap flashes. FINAL PROD (2026-07-04): mobile 85/100/100/100 (LCP 3.7s, TBT 80ms, CLS 0 — was 45/83/77/100 at day start), desktop 95/100/100/100 (LCP 0.7s).**

- **D40 — WALL-OF-FAME STATEMENT TIMEOUT + DATA CORRUPTION (2026-07-04, commit 397e478).** ⚠️ **`certifications` rows carried FULL base64 photos inline (up to 2.8MB/row → ~50MB per SELECT) — the wall-of-fame query hit the Postgres statement timeout (HTTP 500)**, so /achievements showed 0 in all stats + empty wall on first load; reloads "worked" only via localStorage cache. TWO data bugs fixed in production: (1) 11 legacy-import rows had the photo base64 shifted into the `country` COLUMN (photo_url NULL) — cards showed no image + raw base64 text as the country chip (Yatharth/Neel/Nensi); (2) all 5 distinct base64 photos decoded → sips-downscaled to 400px → uploaded to the public `avatars` bucket (`wall/<slug>.<ext>`), 65 rows repointed (45MB inline → 262KB files; full query now 42KB <1s). **Guards:** `ensurePhotoIsUrl()` in google-sheets.ts uploads any data-URI photo to storage before insert (both submit paths use it — profile photos from LoginSignup are base64, so without the guard new rows would re-corrupt); fetchCertifications now settles auth via getSession, retries once, and THROWS instead of silently returning [] (silent [] rendered 0s instead of the error UI). Migration script: scratchpad migrate-photos.cjs pattern (psql json_agg → sips → storage POST w/ service key). Also: stat cards de-iconed (number + hairline + tracked label), card buttons → text-only LinkedIn (outline) + View profile (primary), dead via.placeholder → ui-avatars initials. **Rule: never store data-URIs in table columns — storage bucket + URL only; when a Supabase query "returns nothing" check for statement timeout (500) before blaming RLS.** The PII exposure was closed same-day per the user's "do not disclose any personal data" — see D41.

- **D41 — PUBLIC WALL PII LOCKDOWN (2026-07-04, commit 6979848, migration 037 APPLIED).** Per user directive "do not disclose anyway personal data": **anon column-level privileges on `certifications`** — `revoke select from anon` + re-grant ONLY display columns (id, user_id, full_name, provider, certification_name, exam_code, certification_date, verified_credential_url, linkedin_url, photo_url, country, additional_notes, is_public, created_at). email/phone_number/state_province/city/country_code are now **unselectable by anon at the Postgres level** (verified 42501), not merely unrequested. fetchCertifications selects display columns only (contact fields empty in the payload); CertificationForm dup-checks switched to `getUserCertifications()` (own rows, authenticated) since the public feed no longer carries emails; Achievements' person-grouping key degrades to name-only (same behavior as /yatri/:slug). Authenticated role keeps full columns (admin console + self-service need them; RLS still rows-gates). **Gotcha: PostgREST returns 42501 for the whole query if ANY requested column is unprivileged — when adding columns to public queries, mirror them in the migration 037 grant.** Residual closed same-day by D42's public view.

- **D42 — FULL SECURITY HARDENING PASS (2026-07-04, commit 702f98a, migration 038 APPLIED).** Per "implement recommendation secure it and set all best practices do all": (1) **`certifications_public` VIEW** (definer-rights, owner postgres — its `is_public=true` filter + fixed column list ARE the boundary) is now the ONLY public read path; the `certifications` TABLE policy became `user_id=auth.uid() OR is_admin()` — even signed-in accounts can't read others' contact data. fetchCertifications + CertificationPaths read the view. (2) **Audit of all 61 anon-readable tables found 2 more leaks, both fixed via anon column grants: `trainings.meet_link` + `trainer_email`** (select * shipped Meet links + trainer emails to every visitor!) **and `reviews.email`**. listPublishedTrainings/getTrainingDetail use a session-aware column list (`*` when signed in so enrolled students keep the meet link; TRAINING_PUBLIC_COLS when anon — anon `select *` would 401 outright). (3) vercel.json security headers (nosniff, SAMEORIGIN, referrer-policy, HSTS 2y, permissions-policy) + immutable /fonts cache — verified live. **GOTCHAS: (a) after CREATE VIEW, PostgREST needs `notify pgrst, 'reload schema'` or it 404s (PGRST205); (b) adding a column to public trainings queries requires BOTH the migration-038 grant AND TRAINING_PUBLIC_COLS in training-api.ts; (c) same for new wall columns → add to the certifications_public view.** Audited-and-accepted by design: certificates.recipient_name (public verification), events.meet_link (doubles as the public community link), team_members/mentors public profiles. Remaining residuals: authenticated users can read meet_link of published trainings without enrolling (full seal = private side-table, not worth the refactor now); GTM/gtag CSP not added (nonce-based CSP would need build integration; 'unsafe-inline' CSP is theater).

- **D55 — MANUAL JOB PROFILE + REFERRAL FINDER via Google PSE (2026-07-11, migration 050 APPLIED).** Dedicated `/jobs/profile` (JobSeekerProfile.tsx) — full manual builder: resume upload+view, headline, target roles, work area, seniority, work mode, locations, target companies, notes, completeness bar; job_profiles extended (mig 050). `/jobs/referrals` (JobReferrals.tsx) — EMBEDS the owner's Google PSE widget (cx `22c8ca52ab7fa46e6`, restricted `linkedin.com/in/*`, script cse.js) for manual LinkedIn people search (NO key, NO cost, public results, no scraping) + client-side connection-note & post-accept follow-up generator from profile+company+role. Both linked from board header (My profile/applications/referral). `referral_searches` table created (mig 050) for the JSON-API upgrade. **PSE JSON-API mode (worker ranks profiles + Claude auto-writes notes) BLOCKED on user's GOOGLE_CSE_API_KEY** (+ optional 2nd CX for JOB search across linkedin.com/jobs+careers.google+amazon.jobs+ATS). Full "what to add into the PSE" list + query templates + key setup = docs/features/programmable-search.md. - **D54 — JOB BOARD: BULK IMPORT + ENRICH + LEVEL/HTML FIXES + EMAIL-FOR-ALL + PROFILE (2026-07-11, migrations 047/048/049 APPLIED. READ FIRST for job board).** THREE Mac-worker scripts (service role, keys in .env, never client): (1) `jobs-sync.mjs` — official ATS (Greenhouse/Lever/Ashby/**SmartRecruiters**) + free aggregators (**Arbeitnow/Remotive/RemoteOK** no-key; **Adzuna** env-gated ADZUNA_APP_ID/KEY, India+11, still UNSET — user must add free keys); daily launchd `com.yatricloud.jobs-sync` 09:00. (2) `jobs-import-csv.mjs` — scans `reference/yatri-jobs/*.csv`, detects ATS slug from careers URLs, adds companies (added 152; **1,612 custom-career-site rows skipped honestly = no API**). (3) `jobs-enrich.mjs` — backfills `website`(→favicon logo) + `careers@domain` `contact_email`; ATS companies get `{slug}.com` guess (favicon→initial-chip fallback if wrong). FLOW: CSVs→import→sync→enrich. **SCALE NOW: ~10,900 jobs, 296 companies (101 w/ jobs), 677 India, 179 logos, 174 emails.** FIXES: (a) **levels from the JD not title** — deriveLevel(title,description) parses years-of-exp (0-2 entry/3-5 mid/6+ senior)+early-career wording; Stripe "Software Engineer" wanting 8+yrs now senior; **intern** own level (mig 048)+filter. (b) **HTML in descriptions** — Greenhouse entity-encodes (`&lt;h2&gt;`); stripHtml decodes angle brackets BEFORE stripping tags. (c) **emails for EVERY app** (was gated on contact_email nothing had) — mig 049 `job_applications.email_to`, editable recipient in preview dialog. (d) **job-seeker profile** on /jobs/applications: view-resume, AI "profile at a glance" (summary/level/roles from job_match_requests), pipeline counts; link from board header + launchpad chip (UserRound). **EMAILS = UNVERIFIED careers@ GUESSES** user reviews per-send (never scraped/LinkedIn). **PHASE 4b Gmail auto-send + PDF attach = BLOCKED ON USER**: needs their Google Cloud OAuth client + gmail.send scope + verification — click-by-click guide `docs/features/gmail-send-setup.md`; user gives GMAIL_OAUTH_CLIENT_ID (secret→.env) then fresh session builds connect→worker-send-with-attachment→daily-cap. REMAINING: Adzuna keys, Gmail OAuth build, admin referral_contacts UI, MNC portal adapters. Full ops table in docs/features/job-board.md. UPDATE(same day): ~11,800 jobs; logos now cascade Clearbit→favicon→icon.horse→initial + guess domain from name (JobBoard CompanyLogo), so far more render than the 184 w/ stored website; Sort control added (newest/oldest/title); jobs-import-csv now RECURSES subfolders (new-jobs-lists/ etc); .xlsx lists need soffice --convert-to csv first; real careers emails from "Carrer Contacts" applied to 8 matched cos; Indian MNCs (TCS/Infosys/Google India) are custom portals = no ATS API, not on board.
- **D53 — MORE JOB SOURCES, ALL SERVER-SIDE (2026-07-11, migration 047 APPLIED, user: "found all api, add, no client side, secure way").** From reference/yatri-jobs/Job-and-Company-Data-APIs-Research.md added to `jobs-sync.mjs` (worker, service role): **SmartRecruiters** as a 4th per-company ATS provider; **free aggregator feeds** Arbeitnow / Remotive / RemoteOK (no keys) → grouped into on-demand `source='aggregator'` companies (slug `agg-<name>`, external_id namespaced `<feed>:<id>`); **Adzuna** (India+11, env-gated on `ADZUNA_APP_ID`/`ADZUNA_APP_KEY` in .env, server-only, skipped silently when unset). First run: 4,036 active jobs / 144 companies (117 aggregator). SECURITY: every key stays in .env on the Mac worker, NEVER VITE_ (would bundle to browser); /jobs reads job_postings via anon key + public-read RLS only. Aggregator companies keep jobs_count=0 so they don't flood the company filter dropdown (their jobs still list). Hero copy de-pinned from company count. NOT wired (deferred, need scraping/paid/legal): JSearch, Naukri(Apify), JobSpy(LinkedIn/Indeed). **D52 — JOB BOARD PHASE 4: OUTREACH EMAILS (2026-07-11, migration 046 APPLIED).** Per-application Claude-drafted emails via the resume worker (3rd queue type): dashboard "Draft N emails" sets `job_applications.email_status='drafting'` for apps whose company has a `contact_email` → worker reads JD+profile, writes email.json (subject/body, 110-150 words, no invented facts) → `drafted`. Dashboard preview/edit dialog (subject+body, save or send) + "Open in Gmail". **SENDING = Gmail compose deep-link** (`https://mail.google.com/mail/?view=cm&fs=1&to=&su=&body=`) — opens the user's LOGGED-IN Google account compose prefilled, they click Send. NO passwords, NO OAuth tokens, NO daily-cap block risk (manual send). True OAuth automation deferred — needs the user's own Google Cloud project + gmail.send scope + app verification (multi-day). Send-all opens N compose tabs staggered 600ms + marks sent. `referral_contacts` table (admin-managed, public read, NO scraping — willingly-shared contacts only) created for a later admin UI. Emails only appear where `job_companies.contact_email` is set (admin via /admin/jobs data / a column edit). Signature = profile full_name appended at send. Job board phases 1-4 all shipped; remaining polish: admin referral-contacts UI, MNC custom-portal adapters, Google Sheets CSV import.

- **D43 — COMMUNITY VISUAL = THE LOGO SHAPE, NOT A GLOBE (2026-07-10).** User: the homepage community avatar cluster must form the **white lines of the Yatri Cloud logo** (cloud outline + cheering-Yatri figure), NOT an outer round/circle. Replaced `GlobeVisualization` (3D sphere + rAF rotation, no reduced-motion guard) in `CommunitySection.tsx` with `CloudLogoVisualization`: cloud outline computed at module scope from 3 lobe circles (left/right r26, mid r36) + flat base in a 200×170 design box — lobe seams via `upperIntersection()` circle-circle math, arcs sampled into a closed polyline, rendered as an SVG dotted stroke (`strokeDasharray="0.1 5.5"` round caps = dot chain) over a faint continuous guide; figure = 4 dashed paths (V arms + twin legs) + solid head dot at (100,78); 10 member avatars evenly arc-length-spaced along the outline (`spotsAlongOutline`), positioned by percentage so it scales, gentle staggered y-float (reduced-motion guarded — the old globe wasn't) + spring hover scale. Layer nesting matters: static translate div → float motion.div → hover motion.div (framer overwrites `transform`, so never combine class translates with `animate` on one node). Verified in Chrome at localhost:8080. **D43b — signature choreography added (same day):** cloud outline draws itself on scroll (2.2s linear pathLength on a solid layer that fades → hands off to the dotted layer); avatars pop (spring, 4-layer nest: position→entrance→float→hover) timed to the stroke reaching their spot (delay 0.11+i·0.22); figure draws arms→legs; head springs in + looping radar pings (animate r+opacity); idle: dashoffset flow loop (cycle 5.6 = dash period, seamless) + 2-layer comet via SVG `animateMotion` wrapped in a fade-in motion.g (animateMotion can't delay-hide itself — the wrapper opacity does it). GOTCHAS: (a) framer's pathLength animation writes strokeDasharray — NEVER combine it with a dashed stroke on the same path; draw on a solid layer, fade the dotted layer in after; (b) don't put entrance delay in a shared `transition` prop with whileHover or hover inherits the delay — separate layers; (c) background/occluded tabs throttle rAF so framer loops freeze and bridge screenshots go white — verify via DOM attribute sampling, not screenshots. **D43c — the figure comes ALIVE (user: "fix inner blue lines, something crazy"):** figure geometry cleaned (symmetric V arms M 83 84→98 101 / M 117 84→102 101, legs x 94.5/105.5 y 108–128), strokes now a vertical blue gradient (`#yc-figure-grad`, blue-300→primary, stop-color via inline style so CSS vars resolve); cheer loop = whole-figure hop (y [0,-3,0]) synced with mirrored arm waves (rotate ±7° around the junction via `transform-box: fill-box` + transformOrigin "100%/0% 100%" — bbox corner = junction; identical delay 3.4/duration 2/repeatDelay 1.2 keeps hop+wave in sync); 4 energy sparks loop up the legs and out the arms (animateMotion on REVERSED spark paths + SMIL opacity 0;0.9;0, staggered begins, wrapped in a fade-in motion.g at 3.2s). Head+pings moved INSIDE the bounce group so they ride the hop. Verified via DOM transform sampling (translateY + mirrored rotates present); framer auto-sets transformBox on SVG nodes it animates, so fill-box filter finds 3 groups not 2. **D43d — legs were INVISIBLE + jumping-jack kick (user screenshot showed no legs):** root cause = `linearGradient` default objectBoundingBox units collapse on zero-width shapes → vertical leg lines rendered NOTHING (arms are diagonal so they survived). Fix: `gradientUnits="userSpaceOnUse"` x1/y1 100,70 → x2/y2 100,130. Legs now kick outward ±8° around the hip, synced with hop+wave (same delay/duration/repeatDelay). SECOND GOTCHA: framer OVERWRITES style `transformOrigin` with 50% 50% — must use framer's `originX`/`originY` style props (fractions) instead; verified origins land on junction corners (arms 100%/0% 100%, legs 50% 0%). **D43e — proportion/spacing pass (user screenshot review):** figure enlarged + recentered to fill the cloud interior like the real logo — arms widened to tips (79,80)/(121,80) → junctions (98/102,101), head raised to (100,74.5) r6 so it nests in the V notch, legs lengthened y107–129 toward the base, strokes 3.2, gradient span y64–132, ping rings 6.5→18; container max-w 560→620px for avatar breathing room. **D43f — collision fix (user: photos and lines collapse):** avatar spots are now pushed 7 units radially OUTWARD from the cloud center (100,103) via `pushOut()` so chips ride the line from outside and the interior stays clear; chips downsized h-10/14 → h-9/12; legs shortened to y126 so they clear the base row (base avatars now center y≈141, top edge ≈133). Rule: avatars sit OUTSIDE the outline, figure owns the interior. **SUPERSEDED by D43g (user: photos too small + not exactly on line):** pushOut REMOVED — avatar centers sit EXACTLY on the outline ("beads on the line"); chips restored to h-11/h-14 (44/56px); breathing room comes from canvas size instead (max-w 620→680px). Final rule: chips ON the line, spacing via canvas width, legs end y126 clear of the base row. **D44 — homepage Latest Exam Dumps cards fixed (user: images must be square/contained/centered, boxes aligned):** killed the tilted-deck rotate (−2.5/1.5/−1 read as misalignment) and the 16:10 `object-cover` crop (badge art bled off the edges). Image stage now matches ExamDumpCard on /examdumps: `aspect-square` + `object-contain` centered on a brand-50 gradient wash with p-8. Verified via DOM: square, contained, centered, rotation none ×3. Rule: product/badge artwork is NEVER object-cover — square stage + contain, per the /examdumps convention. **D49 — RESUME MAKER: SITE + LOCAL MAC WORKER (2026-07-11, migration 039 APPLIED).** User chose "my Mac as the worker" (AskUserQuestion) over API/form architectures. `/resume-maker` (lazy route + QuickAccess chip + sitemap): signed-in Yatris paste resume/notes + optional JD → insert `resume_requests` (RLS: authenticated insert/select OWN + is_admin; NO user update — worker mutates via service role) → page polls every 6s → Ready → signed-URL downloads from private `resumes` bucket (owner-folder read policy, path `<user_id>/<request_id>/…`). Worker = `scripts/resume-worker.mjs` (node, reads repo .env service key): claims oldest queued with a guarded PATCH (status=eq.queued so two workers can't double-claim), runs **`claude -p` headless** (`--allowedTools Read,Write`, job dir `~/.yatri-resume-jobs/<id>`) with resume.example.json as schema + best_practices.md as rules to produce resume.json, then `resume-maker/scripts/make_resume.sh` (node docx → python post → LibreOffice pdf), uploads, marks ready/failed. `resume-maker/` engine copied into repo root (node_modules ignored). Prereqs on the Mac: claude CLI + node + python3 + LibreOffice; jobs queue politely while the worker is off. GOTCHA: yatris-api `getStoredUser()` User has NO id — resolve `supabase.auth.getUser()` inside resume-api insert. Zero new api/ functions (Vercel 12-cap). Doc: docs/features/resume-maker.md. Pre-existing bug spotted, not fixed: MyEvents links to `/login` which has no route. **D51 — PHASE 3 SHIPPED: PROFILE + SELECT + BATCH TAILORED RESUMES (migration 045 APPLIED, cap 3→12):** job_profiles (one resume+roles per user) + job_applications (unique user+job, links to resume_requests) + /jobs card CHECKBOXES → /jobs/applications tabular dashboard (profile card, Build N tailored resumes = one resume_request per job with jdText from the stored JD reusing profile resume file, live status poll, Word/PDF downloads, per-company Draft-email mailto when contact_email set else Apply-on-site link, remove). buildSelected stops at insert-cap and resumes next click. PHASE 4 REMAINS: Claude-drafted bodies + OAuth Gmail in-app send + admin referral contacts. **D50c — ADMIN + DAILY SYNC (f556c1e):** /admin/jobs companies CRUD (add name+slug+source, toggle, delete, counts, last-sync; Site Content group) + launchd `com.yatricloud.jobs-sync` daily 09:00 (log ~/Library/Logs/yatri-jobs-sync.log). NEXT SESSION: phase 3 (select matched jobs → batch tailored resumes via resume worker, one per JD) then phase 4 (OAuth-only Gmail outreach) — full spec in docs/features/job-board.md. **D50b — JOB BOARD 1b+2: ASHBY + LOGOS + RESUME MATCHING (migration 044 APPLIED).** Ashby official posting API added to jobs-sync (Notion +141 via ashby; Rippling/Razorpay/Zepto/Swiggy/Zomato are on none of GH/Lever/Ashby under those slugs — custom portals, need adapters or correct slugs via admin). Company logos AUTO via `google.com/s2/favicons?domain=<website>&sz=64` (fallback: initial in a brand chip) — no manual uploads. **Matching flow**: `job_match_requests` (RLS own, cap 2 active) + JobBoard panel (upload resume → poll → "N openings match" → Show-my-matches mode = `.in(id, job_ids)`), worker (same launchd service) processes: claude reads resume → roles.json (roles/keywords/level/summary) → PostgREST `or=(title.ilike.*kw*)` shortlist ≤350 → claude ranks → job_ids ≤40 into result jsonb. lib: job-match-api.ts; reuses resumes bucket via uploadResumeSource. REMAINING (documented roadmap): /admin/jobs CRUD, daily sync launchd, phase 3 batch tailored resumes per selected job, phase 4 OAuth Gmail outreach. **D50 — JOB BOARD PHASE 1 (2026-07-11, migration 043 APPLIED).** Full 4-phase plan in docs/features/job-board.md (match → batch tailored resumes → OAuth-only Gmail outreach; constraints: official ATS APIs only, no LinkedIn/email scraping, NEVER passwords). Shipped: job_companies+job_postings (public-read RLS), `scripts/jobs-sync.mjs` (Greenhouse+Lever official APIs, probes both per slug + self-corrects source, HTML-stripped 15KB JDs, level derived from title, stale rows deactivated per run) — first live sync **3,663 jobs / 19 boards**; /jobs page with SERVER-side search/filters/pagination (never ship the full set to the client; description fetched per-dialog), QuickAccess chip. Failed seeds (Notion/Rippling/Razorpay/Zepto/Swiggy/Zomato = other ATS) + /admin/jobs CRUD + sheet imports = phase 1b. User's reference lists live in reference/yatri-jobs/jobs.txt (Google Sheets links — need CSV export to import). **D49d — QUALITY AUDIT + PREVIEW/EDIT + POLISH (migration 042 APPLIED).** Generated resume quality audited by reading the PDF: template/sections/honesty solid, ONE real defect — "Computer Engineering, undefined" (model omitted institution, builder printed the literal). Fixes: prompt forbids literal undefined/null + omit unknown keys; worker `scrubResumeJson()` deep-cleans resume.json before build; account email now instructed into the contact line. New: **Preview** button (signed-URL PDF in an iframe Dialog), **Edit & rebuild** (Pencil replaces plain rebuild — dialog prefilled with notes+JD, reuses uploaded file, builds a NEW request; original kept), worker emails "your resume is ready" via prod /api/send-email (fire-and-forget), **cap 3 active requests/user** via INSERT policy subselect (042 — RLS subselect counts own rows only, which is correct here). Test entries wiped (5 rows + files). GOTCHA: `const URL=` in node -e shadows the global URL constructor and breaks fetch. **D49c — E2E PROVEN + ONE-PAGE GUARANTEE + LAUNCHD + CRUD (migration 041 APPLIED).** Full flow verified live 4×(incl. the user's own UI submission with uploaded PDF): queue→claim→claude→build→storage→Ready→signed downloads. One-page: hard rule in the first prompt + post-build page count with compaction retry (max 2) — VERIFIED old build 2 pages → new build 1 page. GOTCHA: `mdls` returns null on freshly written files (Spotlight lag) — count pages by parsing the PDF (`/Count N` max, fallback /Type/Page objects). Worker runs as LaunchAgent `com.yatricloud.resume-worker` (RunAtLoad+KeepAlive, PATH env includes homebrew + ~/.local/bin for claude; plist template in scripts/launchd/, installed to ~/Library/LaunchAgents, logs ~/Library/Logs/yatri-resume-worker.log; restart w/ `launchctl kickstart -k gui/$UID/com.yatricloud.resume-worker`). CRUD (041): owner/admin DELETE on rows + own-folder storage delete; admin UPDATE (retry failed→queued clears paths/error); admin storage SELECT; user page gained rebuild/delete icon actions (D24 hover rules); NEW /admin/resumes (Site Content group, §10 trio: search/status/sort/pager, retry/download/delete). **D49b — PDF/DOCX upload (migration 040 APPLIED):** browser uploads straight to `resumes/<uid>/<random>/source.<ext>` (owner-folder INSERT storage policy) + `input_file_path` column; worker downloads with service role — PDF handed to Claude's Read directly, DOCX text-extracted via python stdlib zipfile (headless Claude has no Bash for conversion); client validates .pdf/.docx + 10MB; file OR ≥40-char text required, textarea becomes optional notes when a file is picked. **D48 — HOMEPAGE FLOW OVERHAUL + YATRIS WORLDWIDE MAP + LAUNCHPAD (2026-07-11).** User: worldwide map on home + "redesign home flow per user psychology, quick access to everything". (a) **New order** (buying journey): Hero → QuickAccess(NEW) → Trust(moved up from #7 — proof right after promise) → CertFlow → Voucher → Curriculum → ExamDumps → IndustryLeaders → Instructor → Reviews → **YatrisWorldwide(NEW)** → Recognitions → FAQ → Community → Contact. (b) **QuickAccessSection**: chip rail launchpad (8 destinations, hover lift, hash chip smooth-scrolls to #courses). (c) **YatrisWorldwideSection**: reuses the Achievements map look (react-simple-maps, already a dep) — user explicitly wanted "this"; markers from **`option_lists` list `community_countries`** (admin-editable TODAY via the existing Dropdown Options section — no migration needed) seeded with the 6 countries that have REAL records (IN 92, EG, NG, GT, BD, DZ — verified across certifications/profiles/event_registrations/voucher_requests; D34-honest, no fake dots); centroids from `src/data/country-centroids.json` (generated from Natural Earth 110m public domain via scratchpad gen-world-map.mjs; stored in 0-100×0-50 equirect space, inverse `[x*3.6-180, 85-y*2.9]` back to lon/lat); map is React.lazy + IO-gated (own chunk; entry 532KB) with world-atlas fetched from CDN like Achievements does. To add a country: add an option_lists row (label must match Natural Earth lowercase name or the NAME_ALIASES map in the section). **SUPERSEDED same day (user: "exact map exact UI same as /achievements, achievement map not community map"):** homepage now renders `YatrisWorldwideInner.tsx` — a LINE-FOR-LINE replica of the Achievements World Map section (heading "Yatris, worldwide" + em-dash subcopy kept verbatim, provider filter tabs w/ logos+counts, PROVIDER_MAP_COLORS markers sized by count, legend, band-tint wrapper) fed by the same fetchCertifications; `YatrisWorldwideSection.tsx` is just the IO(800px)+lazy gate so react-simple-maps stays out of the entry (525KB). Deleted the dot-matrix version (YatrisWorldMap.tsx, country-centroids.json, world-dots). community_countries option list kept in DB+fallback (unused for now). RULE: when editing the Achievements map, mirror in YatrisWorldwideInner (comment in both… in Inner at least). **D47 — CHECKOUT CONVERSION REDESIGN (2026-07-11, user: "best ever to make sales, psychology based").** /examdumps + CartSheet tuned with honest conversion psychology (every claim real per D34): (a) hero trust chips (instant email delivery, secure Razorpay, {learners} from site_stats) + a 3-step "how it works" strip (pick → pay → inbox) to kill digital-purchase anxiety; (b) **Buy Now now BUYS**: ExamDumpCard adds to cart then fires `yc:open-cart` (+ sessionStorage `yc:open-cart-pending` one-shot for lazily mounted listeners) → the page's CartSheet opens instantly; CartSheet gained optional `openOnBuy` prop — enable on exactly ONE instance per page (ExamDumps header sheet + FloatingCart pill; NOT MobileCartBar or sheets stack); (c) CartSheet: "You save today ₹X" anchoring line (display-only reduce over originalPrice), checkout button → "Pay {total} securely" with ShieldCheck (amount-on-button), flex-[2] dominant vs Clear Cart, trust rows under the button (Razorpay/UPI/cards + delivery-to-email), cart thumbnails object-contain on muted (logos were cropped), empty state gained a Browse-exam-dumps CTA; (d) FloatingCart pill hidden on /examdumps too (page has its own cart UI + MobileCartBar). E2E-verified: Buy Now → sheet opens with all elements. **D46 — UDEMY BASE64 IMAGE PURGE (2026-07-11, data-only fix, live immediately).** Same disease as D40: all 18 `udemy_courses.image_url` rows carried inline base64 (~15MB total, up to 837KB/row — legacy-import residue; BOTH current write paths, public Udemy.tsx submit and UdemyAdmin editor, already upload to the product-images bucket properly). Homepage course cards showed the SAME unsplash earth fallback because hydrating 6 cards dragged ~2MB+ of base64. Migrated via scratchpad migrate-udemy-images.mjs (D40 pattern: service-key REST fetch → decode → sips 640px → storage POST product-images/udemy/<id>.png → PATCH row): 18/18 OK, 0 base64 remaining, anon id+image_url payload 15MB → 3.4KB, storage URLs serve 200. Header nav also centered at lg (7e95480) and Practice Tests removed from navbar (4d7ebf3: fallback + nav_links row active=false — footer_quick copy kept). **D45 — FLOATING CHECKOUT PILL, site-wide (user: after Add to cart there must be an obvious way to go purchase).** Problem: CartSheet (the full checkout — coupons/currency/Razorpay/emails) was mounted ONLY on /yatristore; adding from the homepage section or /examdumps gave a toast and a dead end. Fix: new `src/components/store/FloatingCart.tsx` — fixed bottom-right pill (cart icon + count bubble + "N items · Checkout ₹total") that wraps CartSheet with a custom trigger; pops in with spring and RE-pops on every add (key={totalItems} remount); reduced-motion + 44px + aria-label. Mounted via `FloatingCartGate` in App.tsx next to CalendlyPopup: renders ONLY when totalItems>0, hidden on /yatristore (own cart UI + MobileCartBar) and /admin|/trainer; the pill component is `lazy()` so checkout code stays out of the entry bundle (D36 discipline). E2E-verified in browser: homepage Add → pill appears bottom-right → click → full checkout sheet opens. Bottom-left is CalendlyPopup's Schedule Meet; bottom-right now belongs to the cart pill.

- **D56 — ADMIN COMPLETENESS PASS: store products, trainer RLS, moderation, inquiry delete (2026-07-14, commits b73826f + b3ce36e, migrations 067/068/069 APPLIED).** Per "make sure everything from admin can manage a to z" + "do all 3 steps" + "next recommendation": (1) **AdminProducts** (`/admin/products`, Store & Catalog nav) — list/search all products, edit dialog (title/category/level/prices/examCode/image/description), publish toggle, delete; store-products.ts gained listAllProducts/updateProduct/setProductStatus/deleteProduct (products_admin_write RLS already ALL is_admin). (2) **Trainer auth hardening** — mig 067: `trainings_trainer_insert` (own DRAFT rows, role in trainer/admin) so approved trainers can finally CREATE courses (no insert policy existed!) + WITH CHECK on `trainings_trainer_write` blocking self-publish past admin review; **TrainerDashboard now verifies live `fetchMyProfile()` role on mount** instead of trusting localStorage `trainerData`. (3) **AdminReviews** (`/admin/reviews`, Site Content nav) — moderate the site-wide `reviews` wall: hide/show via is_public + delete; mig 068 added the missing `reviews_admin_delete` policy (only admin UPDATE existed). (4) **Inquiries delete** — AdminInquiries gained a delete action (confirm dialog) on BOTH Partner-inquiries and Contact-messages tabs; mig 069 added `consult_admin_delete` + `contact_admin_delete` (both tables had only SELECT/UPDATE). **PATTERN CONFIRMED: several legacy tables shipped with admin SELECT/UPDATE but NO DELETE policy — a moderation "delete" always needs `create policy X for delete using (is_admin())` first (DDL via psql, service-role REST can't).** `/creator`→`/trainer` rename shipped earlier this session (redirect kept for SEO). Monetization/Stripe still deferred per user. Remaining recs: AddProduct auth unify, per-course SEO, more granular trainer course editor.

- **D57 — REMAINING ADMIN/TRAINER RECS: validation unify, course SEO sitemap, id-preserving course save (2026-07-14, commits 08a4e5a + 97cbe87).** Per "do all remaining recommendations": (1) **Store-product validation unified** — the product Zod schema + `STORE_CATEGORIES`/`PRODUCT_LEVELS` now live in `store-products.ts` as the single source of truth; AdminAddProduct imports it (no local dup) and now redirects to `/admin/products` after adding; AdminProducts' edit dialog validates via new `validateProductPatch` (non-empty title, image-if-present must be URL, non-negative prices, offer ≤ list) instead of only title. Both surfaces already shared admin gating (AdminDashboard route guard: `fetchMyProfile().role==='admin'` + `products_admin_write` RLS is_admin) — no per-page check needed. (2) **Per-course SEO** — per-page `<SEO>` meta ALREADY existed on `TrainingDetail.tsx` (title/desc/og/Course JSON-LD, noindex for private); the only gap was crawler discovery. Added `scripts/gen-courses-sitemap.mjs` (mirrors gen-blog-sitemap: service-role read of `trainings` where status=published + visibility≠private + slug present → `public/sitemap-courses.xml`, `/training/:slug` URLs), folded blog+courses into ONE daily workflow renamed **"SEO sitemaps"** (`.github/workflows/blog-sitemap.yml`), added 3rd `Sitemap:` line to robots.txt. **trainings table is currently EMPTY (0 rows) → sitemap lists only /training; self-populates as courses publish.** (3) **Trainer course editor made production-grade** — the granular editor already existed (`TrainerCourseEditor.tsx` at `/trainer/course/:courseId/edit`, wired from TrainerDashboard) but `saveCourseContent` did delete-all→reinsert; since `lesson_progress.lesson_id` FKs `course_lessons.id` **ON DELETE CASCADE**, every trainer save silently WIPED all enrolled students' completion progress. Rewrote it id-preservingly: UUID moduleId/lessonId → update in place (ids stable), temp `MOD…/LSN…` ids → insert, only trainer-removed rows deleted. RLS already allows it (`modules_trainer_write`/`lessons_trainer_write` are FOR ALL, ownership-scoped) — no migration. **KNOWN REMAINING FOOTGUN (documented, separate pass): the coarse `TrainingManager` path (updateTraining→saveCurriculum) still replaces curriculum wholesale AND loses per-lesson url/description, because `getTrainingForEdit` projects modules/lessons without ids or content — so editing course metadata via the coarse admin/trainer form still nukes granular content + progress. Fixing needs threading ids+content through TrainingInput/TrainingManager (unify the two editors).**

- **D58 — TWO COURSE EDITORS UNIFIED ON ONE ID-PRESERVING SAVE (2026-07-14, commit 3c1443b).** Closes the D57 footgun. The coarse editor (`TrainingManager`, used by admin `/admin/training/create|edit` + trainer `/trainer/course/create`) and the granular editor (`TrainerCourseEditor`, `/trainer/course/:id/edit`) write the SAME `course_modules`/`course_lessons` rows, but the coarse path (`updateTraining`→`saveCurriculum`) still did delete-all→reinsert — wiping student progress (`lesson_progress.lesson_id` FKs `course_lessons` ON DELETE CASCADE) AND the per-lesson `url`/`description` the coarse form doesn't even edit (so editing a course's price destroyed its granular content). Fix keeps BOTH UIs, unifies the persistence: (1) `getTrainingForEdit` now carries `moduleId`/`lessonId` into the form curriculum; (2) `TrainingManager` round-trips them via **hidden registered `<input>`s** (`curriculum.${i}.moduleId`, `…lessons.${j}.lessonId`) so react-hook-form keeps them on submit — modules/lessons added in the form have no id → treated as new; (3) `saveCurriculum` now mirrors `saveCourseContent` — UUID rows UPDATE in place (ids stable), new rows insert, only removed rows delete, and for an existing lesson it **MERGES content** (`{...prevContent, type, duration}`) so stored url/description survive a coarse-form save. **PATTERN: distinguish existing vs new rows by `UUID_RE.test(id)` — editor-created temp ids are `MOD…`/`LSN…`, DB ids are UUIDs; single `UUID_RE` const above `saveCurriculum` serves both save functions.** Proven with a service-role integration test (synthetic course; algorithm is identical whoever runs it): lesson id stable, lesson_progress survives, content.url+description preserved, no dup rows — then cleaned up (trainings table back to 0). Markdown import still intentionally replaces curriculum (imported rows have no ids = all new).

- **D59 — WHOLE-WEBSITE HARDENING/POLISH PASS: RLS audit, tests, bundle, SEO, casing, auth (2026-07-14, commits 03118ec + 4c266bc + 1b7f993, migration 070 APPLIED).** Six overall recommendations, all shipped. (1) **RLS "no DELETE" audit** — systematically checked all 78 public tables (`pg_policies` × grep of every `.from(x).delete()` call site): found exactly 2 admin deletes silently failing on a missing policy — `training_enrollments` (AdminEnrollments `deleteEnrollment`) + `trainer_applications` (AdminTrainers `deleteTrainerApplication`); migration 070 adds admin DELETE to both. Every other `.delete()` target already had DELETE or ALL. **Did NOT speculatively add policies to no-delete tables with no delete UI (event_registrations/submissions/feedback, voucher_requests, subscribers, course_requests, quiz_attempts, orders) — evidence-based, not blanket.** (2) **Tests locked in (NEW: vitest added)** — `npm run test:unit` (vitest, reuses vite @ alias, CI-safe since supabase client degrades to placeholders on missing env): `validateProductPatch` + `canonLessonType` unit tests (10 pass). `npm run test:integrity` = `scripts/tests/curriculum-integrity.mjs` (seeds course+module+lesson+progress via service role, runs the save, asserts id-stable/progress-survives/content-preserved, cleans up) — separate from CI (needs service key). Playwright e2e stays `npm run test:e2e`; vitest.config.ts excludes e2e/. (3) **Bundle split** — vite manualChunks now a FUNCTION splitting react/supabase/framer-motion/@radix-ui/lucide into long-lived chunks; **critical: return undefined for the rest (no catch-all "vendor")** — a catch-all pulled lazy-only libs eager and ballooned to 888KB; leaving them to Rollup defaults keeps lazy libs in route chunks. Main app chunk 537→474KB, size warning gone. (4) **Sitemap index** — `public/sitemap-index.xml` refs page+blog+courses sitemaps; robots.txt now points at the single index. (5) **Lesson-type casing** — `canonLessonType()` (exported from training-api.ts) Title-cases in BOTH save paths so the coarse `<select>` round-trips; granular editor icon check now `.toLowerCase()`-safe for legacy rows. (6) **TrainerCourseEditor auth** — now `await fetchMyProfile()` role-checks on mount (mirrors TrainerDashboard); RLS still the real boundary. **Earlier same session (D57/D58): recommendations #1–3 = AddProduct validation unify, per-course SEO sitemap, and the two course editors unified on one id-preserving save.** Production deploy verified live (sitemap-courses.xml 200, robots 3 lines→now index); trainer editor UI flow couldn't be driven E2E (Google-OAuth login = user-only, and trainings table is empty) but the id-preservation invariant is proven directly against the live DB.

- **D60 — TWO COURSE EDITORS MERGED INTO ONE (Option B, 2026-07-14, commit 7c75de2).** Retired the standalone granular `TrainerCourseEditor.tsx`; **`TrainingManager` is now the SINGLE course editor for admin AND trainer** (one screen, one save path `saveCurriculum`). Rationale: two editors writing the same `course_modules`/`course_lessons` = two code paths to sync (source of the D57 progress-wipe bug), a confusing two-screen flow, and admins who couldn't set a lesson video URL. TrainingManager already did everything equal-or-better (quizzes persist to the `quizzes` table vs the old LOCAL-ONLY stub; resources have file upload; D58 id-preserving save). Only 2 gaps closed: (1) **per-lesson Content URL + description** inputs added to the curriculum tab (ModuleItem) — threaded form→TrainingInput→`saveCurriculum` (which already persisted url/description); `getTrainingForEdit` now loads them (+ `url`/`description` on read) so edits pre-fill. (2) **online Meeting link** field (`trainings.meet_link`) — added to TrainingForm + Logistics online block (replaced the old "created automatically" static note), `TrainingInput.meetLink`, `inputToRow` (**conditional `...(meetLink !== undefined ? {meet_link} : {})` so a save never wipes an auto-generated link from callers that omit it**), and `getTrainingForEdit`. **Routing:** `/trainer/course/:courseId/edit` → new thin `TrainerEditCourse.tsx` wrapper (mirrors AdminEditTraining: loads via getTrainingForEdit, verifies live profile role, `isTrainerMode`, Navbar chrome). Deleted TrainerCourseEditor + the now-orphaned `saveCourseContent` (its only caller); `getCourseContent` STAYS (StudentTrainingDashboard + TrainingDetail render curriculum from it). Verified: typecheck + build + 10 vitest + curriculum-integrity all green. **NOT UI-smoke-tested live** (dev server was down + trainings table empty + trainer login = Google OAuth = user-only) — compile+test evidence only; offer a browser walkthrough once a course exists / dev server up. This completes the D58 "unify safely" → D60 "unify fully" arc; the coarse/granular split no longer exists.

- **D61 — TRAINER-ACCESS BUG: approved trainer denied login (stale cached role) (2026-07-14, commit a13b3ef).** User: "approved trainer but still dont access." DIAGNOSED via live DB: approval WORKED (`approveTrainer` correctly set `trainer_applications.status='approved'` AND `profiles.role='trainer'`; profiles UPDATE RLS `((id=auth.uid()) OR is_admin())` allows admins to set other users' roles — verified `yatricloud@gmail.com` = role 'trainer' in DB). Root cause was the LOGIN GATE: `verifyTrainerAccess` (TrainerLogin's check) read `getCachedUser().role` — the `yatri:user` localStorage mirror — which is STALE (`signInWithPassword` seeds it with a provisional `role:"yatri"` and only refreshes via background `fetchMyProfile()`), so a trainer approved AFTER their last sign-in was denied though the DB said trainer. Fix: `verifyTrainerAccess` now `await fetchMyProfile()` (live role, same source as the TrainerDashboard/TrainerEditCourse guards; falls back to cache only if unavailable) and accepts admin OR trainer. Also hardened `approveTrainer` to NOT downgrade an account that is already admin (`profile.role !== "admin"` guard). **REMAINING DESIGN NOTE: the trainer portal's Google popup (@react-oauth/google → googleapis userinfo) is SEPARATE from the Supabase session; verifyTrainerAccess→fetchMyProfile needs a live `supabase.auth.getUser()` session, so the approved trainer must ALSO be signed into the main Yatri Cloud app (Supabase) with the same email — else "Please sign in to Yatri Cloud first."** Verification shortcut: admins can exercise the (now-unified, D60) course editor via `/admin/training/create|edit` — same TrainingManager, no Google/trainer login needed.

- **D62 — COURSE CREATE/EDIT FORM FIXES + FIRST AZURE COURSE (2026-07-14, commits 1d830bb/26bcd86/a13d2d6).** While verifying the unified editor (D60) live, found + fixed several real TrainingManager bugs: (1) **Course/Exam Name was a dropdown of existing-course names** → empty with 0 courses, impossible to name the first course; now a **free-text input with a datalist of the certification catalog** (certOptions, provider-filtered, falls back to full catalog). (2) **"Certification drives it"** (user chose this): picking a certification auto-derives the **Provider (read-only)** and fills the **Course/Exam Name** (editable) — no duplicate provider step; the redundant manual "Provider Name" pick is gone; effect on `selectedCert.id`, provider matched via training_providers slug/name→display name; "Prepares you for" card relabelled "Certification" for cert courses. (3) **BUG: Radix `<Select value={watch(...)}>` doesn't accept an async-set value** — populateForm runs after the row loads, so on EDIT the Training Type showed "Select type" and Certification showed "No certification" though saved (and controlling Training Type with `value` actually EMPTIED the field, hiding dependent inputs). **FIX PATTERN: uncontrolled `defaultValue` + `key={`type-${selectedType}`}` remounts the select once the value arrives** — applies to any Radix Select populated async. Verified live: Training Type "Certification", Provider "Azure", Course/Exam Name + linked cert all render on edit. **Created the first Azure course** (id 1d56a9fa-…, slug az-900-azure-fundamentals-…, DRAFT, provider AZURE, cert AZ-900, 1 module + 3 lessons with url/type/duration/description, meet_link set) via a service-role script mirroring inputToRow — visible at /admin/training and editable at /admin/training/edit/<id>; publish from the admin UI to make it live. **Browser-automation limit noted: Radix Selects AND Tabs don't respond to synthetic clicks/events — verify Radix-heavy forms via DOM reads (`javascript_tool`) + real screenshots, or have the user drive; couldn't activate the Curriculum tab via automation (data confirmed correct via DB + node instead).**

- **D63 — COURSE PAGE RE-SKIN + TRAINER-LOGIN MISMATCH FIX (2026-07-14, commit 6d35c8d).** (1) **TrainingDetail.tsx re-skinned** to the design system: was an old Udemy clone with hardcoded `#1c1d1f`/`#2d2f31`/`text-gray-300`/`text-white` → in light mode a dark hero slammed against a white body. Now hero = branded band (`from-primary/[0.08] via-brand-50/50 to-background` + blurred blobs, `font-display` foreground title, muted desc, primary-tinted "Prepares you for" card); sidebar = `bg-card` + `border-brand-100` + brand-tinted thumbnail + `bg-primary→brand-600` Enroll button. Lower sections already used tokens. Microsoft(#0078d4)/AWS(#FF9900) brand badges kept intentionally. Design-only. Verified light mode live. (2) **Trainer login "This Google account doesn't match your Yatri Cloud sign-in"** — the trainer portal's Google popup does NOT establish a Supabase session; verifyTrainerAccess just verifies the EXISTING session. It was hard-failing on a Google-email vs session-email mismatch, blocking approved trainers/admins for no security benefit (session role + RLS is the real gate). **Removed the email-match throw**; kept role gate. **DEEPER FLOW STILL AWKWARD (flagged, not yet done): "Continue with Google" is decorative — it needs a pre-existing Yatri Cloud session; a no-session user gets "Please sign in to Yatri Cloud first". Proper fix = Supabase Google OAuth (signInWithOAuth) so the button actually signs in; needs Google provider configured in Supabase dashboard.** Chain: D61 (stale cached role) → D63 (email-match) both on this flow.

- **D64 — TRAINER PORTAL: REAL SUPABASE GOOGLE SIGN-IN (2026-07-14, commit ba14bb8).** Closes the D61/D63 trainer-auth arc. Rebuilt `TrainerLogin.tsx`: "Continue with Google" now ESTABLISHES a Supabase session instead of only verifying a pre-existing one. Uses **Google Identity Services directly** (`loadGoogleIdentity` from `@/lib/third-party` → `window.google.accounts.id.initialize/renderButton`) — the SAME flow as the main login (`LoginSignup.tsx`) — gets the Google ID-token `credential`, exchanges via **`signInWithGoogleIdToken` → `supabase.auth.signInWithIdToken({provider:'google'})`** (Google provider already configured in Supabase, VITE_GOOGLE_CLIENT_ID), then `verifyTrainerAccess()` gates on the live role. A signed-in non-trainer keeps their session, just sees "no trainer access". Dropped the `@react-oauth/google` GoogleOAuthProvider wrapper (GSI needs none; BecomeTrainer still uses useGoogleLogin for the APPLICATION email only). Re-skinned to design tokens (bg-card/font-display). **GSI button verified rendering on localhost (pill, Google logo) → client id valid + localhost is an authorized JS origin.** Couldn't complete the actual Google account-picker via automation (Google-controlled + OAuth is user-only). Full arc: D61 stale-cached-role → D63 drop email-match → D64 real session.

- **D65 — TRAINER DASHBOARD BLUE-THEMED POLISH (2026-07-14, commit f95e107).** Header → branded band (from-primary/[0.08] via-brand-50/60 to-card + blobs, "TRAINER" eyebrow, gradient-text name, +Create button). Stat cards → editorial: Total Courses is a filled BLUE anchor (gradient primary→brand-600, white icon chip); Published Live = success radio chip; Drafts = amber FilePen chip; font-display tabular-nums + hover lift. Design-only. Verified live on localhost.

- **D66 — JOBS IN NAV + BRAND-BLUE TABLE HEADERS APP-WIDE (2026-07-14, commits 137f6d8 + 46e3e01).** Added `/jobs` to the navbar. Brand-blue table headers globally via the shared `Table` component so every admin/data table reads on-brand. Design-only.

- **D67 — PUBLIC DESIGN PASS + AUTO-FIT CARD GRID (2026-07-15, commits 471009a, 41e1e11, 2139d6a, 4f6776b, 184a68a).** Re-skin of the main public pages: branded blue header bands (from-primary/[0.08] via-brand-50/50 to-background + blobs), decorative icons + templated eyebrow labels STRIPPED site-wide (user: "looks cheap/AI-generated"), course detail page Udemy-style polish, BecomeTrainer icon-free + display font. **PATTERN: training/events card grids use `grid justify-center [grid-template-columns:repeat(auto-fit,minmax(min(100%,320px),360px))]` so a lone course/event CENTERS instead of stranding left with an empty right** — this, not container width, was the "everything on the right side" complaint. A mid-session over-widen of the container (1400→1600) was reverted; net kept = these. Design-only. **Caution: an earlier full `git reset --hard 46e3e01` + force-push wiped a day of design churn before re-applying the good parts via cherry-pick — honor explicit rollback requests, but cherry-pick the keepers back.**

- **D68 — EMAIL SYSTEM: BRANDED ENROLLMENT EMAIL, BRAND-BLUE REBRAND, DB TEMPLATE SYNC (2026-07-15, commits 92a053e + 211a06c + 79559cb, migration 071 APPLIED).** New `getEnrollmentEmail()` on the shared `BASE_TEMPLATE` (was a bare unbranded `<div>`); mentorship's `buildBookingConfirmationEmail` refactored to reuse the SAME shell (dedup, gains footer). `COLORS.secondary` navy `#1e3a8a` → brand blue `#007CFF` (header band + headings), footer social icons → `#007CFF`, community link `community.yatricloud.com` → `yatricloud.com/community`. The admin-managed `email_templates` DB rows were rebranded to match (migration 071 = `replace()` on body_html + seeded `enrollment_confirmed`) so admin sessions render the same as the code fallbacks. Also: div-in-`<p>` `DialogDescription` nesting fixed (inline-flex spans), and a **plain-text alternative** is now derived from HTML server-side in BOTH `server.js` and `api/send-email.ts`. **NOTE: local `npm run dev` has no `/api` backend → enrollment emails only send under `dev:all`; prod Razorpay showed a Test-Mode ribbon — confirm LIVE keys.**

- **D69 — TRAINING E2E HARDENING + TEST HARNESS (2026-07-15, commits 3d8307e + d38b3e7, migration 072 APPLIED).** Full UI E2E pass over training (admin+user) surfaced and fixed: **(#1)** enrollment form's phone/city/state/country/linkedin were collected-then-discarded → migration 072 adds columns, `enroll()` stores them, admin list prefers them over the profile; **(#2)** publishing now requires a real title (no more untitled "Training" going live; drafts lenient); **(#4, the big one)** `checkEnrollment` used `.maybeSingle()` which ERRORED on 2+ rows (settled + stale-pending, or two emails) and locked enrolled learners out — now reads all rows, grants access if ANY is settled, which also grandfathers free→paid price changes; **(#5)** empty modules (0 lessons) pruned on publish; **(#6)** "Write a review" CTA for enrolled learners on the detail page. Plus **data-testids** across the training-admin controls (Radix Selects/Tabs, curriculum field-arrays, row ⋮ menu, reviews Switch) + a Playwright authenticated `admin` project (`e2e/auth.setup.ts` signs in via the app's own Supabase client; `e2e/training-admin.spec.ts`). **PATTERN: shadcn/Radix controls ignore untrusted synthetic events (isTrusted:false) — not a bug; drive them with Playwright (trusted input) via data-testids, not JS .click().** RLS on `training_enrollments` (`enroll_read_own`: owner/admin/trainer) already protects the new PII columns — verified.

- **D70 — TRAINING UI TWEAKS (2026-07-15, commit 9636271).** Dropped the "4.8★ average rating" hero trust cue (per request). Course LIST cards now fall back to the SAME blue gradient cover as the detail page (`from-primary via-brand-600 to-brand-700` + provider label + `certificationExamCode || courseName`) instead of a generic book icon — image-less courses look intentional. Design-only.

- **D71 — TRAINING DEFERRED REFACTORS (2026-07-15, commits 229a645 + <this>, migration 073 APPLIED).** (1) Split the 1,440-line `TrainingManager.tsx`: extracted `training-form.ts` (types + TYPES/LEVELS/LESSON_TYPES/STEPS/TIME_SLOTS) and `CurriculumEditor.tsx` (the modules/lessons field-array editor) → main file 1,279 lines. Left the state-coupled form tabs (Identity/Details/Logistics/Review) in place ON PURPOSE — a FormProvider decomposition threads ~15 local-state pieces for single-use JSX = high-risk churn on a critical flow, not worth it. (2) Typed lesson columns: migration 073 adds `lesson_type/duration_minutes/content_url/description` to `course_lessons` + backfills from the `content` jsonb; `saveCurriculum` now writes them in sync with `content` (additive — `content` stays the read source, so no read-path/id-preserving-merge changes). `duration_minutes` = leading digits of the free-text duration ("5 mins"→5). Enables SQL aggregation (e.g. total course duration). Verified: typecheck+build, backfill (AZ-900 3 lessons), and a row accepts the new write shape.

- **D72 — EVENTS E2E SUITE + TWO REAL BUGS (2026-07-16).** Full fixture-backed events coverage: `e2e/events.spec.ts` (public: listing/search/detail, anon register→login-modal, anon past-gallery lock, private-event unlisted, my-events gating, feedback form) + `e2e/events-flow-admin.spec.ts` (register w/ **confirmation-email payload asserted via `page.route("**/api/send-email")` interception** — code `WORKSHOP-XXXXXX` captured and reused, My Events, sold-out waitlist join, admin console row actions, registrations list, gallery console + admin gallery unlock, attendee check-in verify→confirm→duplicate-flag, venue submission→admin approve, UI delete w/ `window.confirm` via `page.on("dialog")`). Fixtures = `visibility:'private'` service-role throwaway events (`e2e/helpers/events-fixture.ts`), cascade-cleaned. **Bug 1 (product):** `getRegisteredEvents()` had no user filter — RLS lets admins read ALL registrations, so admins saw everyone's events as their own and sold-out events showed "You're in" instead of the waitlist button; fixed with `.eq('user_id', uid)`. **Bug 2 (product):** CreateEvent category was free-text — now a Select fed by the global `event_category` option list (admin-editable at /admin/site), legacy values kept as an extra option when editing. GOTCHAS: toast + panel often share text → strict-mode violations, assert the unique companion element; AdminEvents rows are divs `event-row-<slug>`, not `<tr>`; data-dependent specs must skip (not fail) on empty tables — events + trainings tables were emptied by manual testing on 2026-07-16. CI: repo GitHub vars (`VITE_SUPABASE_*`, `VITE_GOOGLE_CLIENT_ID`) + secrets (`E2E_ADMIN_EMAIL/PASSWORD`) were missing — set via API; e2e job green.

- **D73 — SITE-WIDE E2E SMOKES + LINK/STATE FIXES (2026-07-16).** `e2e/site-public.spec.ts` (23 tests: store+cart, examdumps, udemy admin-gate, reviews+feedback, wall-of-fame+login gate, reset-password, jobs+applications gate, resume-maker gate, paths, community, partners, legal, 404, homepage, ⌘K global search, receipts/certificates/edit-profile gating) + `e2e/site-admin.spec.ts` (12 tests: every remaining admin console renders heading+control). Suite total now 68 tests, all green vs empty prod tables. A headless route crash-sweep (43 routes as admin) found NO blank/crashed pages. THREE product fixes: (1) `MyEvents` "Sign in to continue" linked to nonexistent `/login` → opens `LoginModal` (there is NO /login route — LoginSignup lives in /certifiedyatris + LoginModal); (2) `MyPurchases` empty-state "Visit the store" → `/store` (404) → `/yatristore`; (3) `TrainingDetail` never called `checkEnrollment` on load — enrolled learners saw "Enroll for Free" after refresh; added on-load check → "Go to Training". Per user: mentorship + training deep-flow suites SKIPPED (user tested those manually). Known non-bugs: GSI origin warning on localhost (Google OAuth origins, user-side), /jobs external logo DNS noise.

- **D74 — TRAINING + MENTORSHIP DEEP-FLOW E2E (2026-07-16).** Suite now 90 tests. `training.spec.ts` + `training-flow-admin.spec.ts`: publish-guard (no-title blocked; publish button lives on the builder's REVIEW tab only), builder→"Publish Live"→public catalog→free enrollment (email subject `Enrollment Confirmed: {course}` intercepted; EnrollmentModal auto-submits when profile complete — handle both paths; scope form fills to the DIALOG, footer newsletter input also matches /Email/), detail remembers enrollment after reload (regression for the D73 fix), dashboard Mark-complete, My Trainings, /admin/enrollments, UI delete + service-role backstop (`delete trainings where course_title=X`, children cascade). `mentorship.spec.ts` + `mentorship-flow-admin.spec.ts` + `helpers/mentorship-fixture.ts`: service-role fixture mentor (published) + FREE digital service (zero-payment booking path, RLS allows own confirmed amount-0 insert) + free call service + 7-day availability; directory→profile→anon "Sign in to book" gate; booking + email `Booking confirmed: {title} with {mentor}`; SlotPicker (native role=tab dates + aria-pressed time buttons); admin bookings Manage dialog status→completed ("Saved" toast); My Bookings review widget (rating DEFAULTS to 5 — custom role=radio buttons, don't .check()) → "Thank you, Yatri" → /admin/mentorship/reviews. Cleanup deletes children first (reviews→bookings→availability→services→mentor_private→mentors).

- **D75 — EVENT PUBLISH WAS BROKEN + FRESH CONTENT SEEDED (2026-07-16).** Root cause of the empty events table: `eventToRow` wrote `price_inr: null` for FREE events → NOT NULL violation → **every UI event publish failed** since the price mapping refactor (fixture-based e2e missed it — fixtures insert via service role). Fixed (`null`→`0`) + regression test "an event publishes end to end through the builder UI" in events-flow-admin.spec.ts (drives the real builder: category Select, calendar popover next-month day pick, time Select, publish toast "Event Published!"). Seeded LIVE content through the admin UIs: 2 upcoming public Bengaluru events (Azure Fundamentals Bootcamp Jul 25, Cloud Careers Night Jul 31) + 2 published courses (AZ-900, SAA-C03 w/ modules+lessons). Suite now **91 passed, 0 skipped** (data-dependent tests active again). CreateEvent gotchas: publish requires name+date+time+description+aboutEvent (organizer prefilled); calendar = rdp gridcells (leading/trailing month days duplicate numbers — use `.last()` or hop to next month); time options are 12-hour labels ("10:00 AM").

- **D76 — UNIFIED CONTENT REVIEWS (2026-07-16, migration 075 APPLIED live).** One polymorphic `entity_reviews` table for events/products/udemy_courses/exam_dumps (trainings + mentors KEEP their dedicated gated tables). RLS: public read `is_public`, insert own (authenticated), **event reviews DB-gated to non-cancelled registrants** (subquery in the insert policy), owner/admin update+delete. One review per Yatri per entity (`unique(entity_type,entity_id,user_id)`, submit = upsert → writing again edits). Lib `src/lib/entity-reviews.ts` (list/summarize/getMine/submit + admin listAll w/ per-type entity-name resolution, setPublic, delete). Reusable `src/components/reviews/EntityReviews.tsx` (avg★+count header, list w/ show-all, write dialog: role=radio stars default 5 + textarea, LoginModal when anon; testids entity-review-*). Wired: EventDetail "Reviews" TAB, store ProductCard details dialog, ExamDumpCard details dialog. Admin: `/admin/content-reviews` (AdminContentReviews.tsx, nav "Content reviews" under the reviews group; type chips Events/Store/Udemy/Exam-dumps + public/hidden + search testid content-reviews-search; Hide/Show + AlertDialog delete). NOTE: entity_reviews has NO FK to entities (polymorphic) — fixture cleanup must delete reviews explicitly (`deleteEntityReviewsFor`); admin list shows "(deleted)" for orphans. **Udemy courses have NO public page** (/udemy is admin-gated since the legacy strip) — udemy reviews are schema+admin-ready but have no public surface until a listing exists. e2e `reviews-flow-admin.spec.ts` (event review via registered admin, product review via store dialog, admin hide/show/delete). Suite 94 tests.

## Inspiration sources
awwwards.com · motionsites.ai · 21st.dev · getdesign.md · aitmpl.com · `npx getdesign@latest add lovable`.

## Open questions / TODO
- Confirm redesign rollout order (design-system layer → homepage → shared shell → inner pages → dashboards/admin).
- Decide whether to change the **default theme** (currently dark) or keep it.
- Optional: a distinctive display face for hero headings (currently Inter Tight throughout).
