/**
 * Feature Flags Configuration
 * Set any flag to `true` to enable the respective section across the site.
 */
export const FEATURE_FLAGS = {
  myDashboard: false,
  events: false,
  trainings: false,
  userGuide: false,
  userSitemap: false,
  myBlogs: false,
} as const;
