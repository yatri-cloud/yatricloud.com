// Unified footer — re-exports the single canonical footer so every page
// (Training, TrainingDetail, RequestVoucher, TrainerLogin, …) uses the same
// design. The previous standalone dark footer was retired.
export { Footer, default } from "@/components/sections/Footer";
